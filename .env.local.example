VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Integration tests only. Direct postgres URL of a THROWAWAY Supabase project/branch.
# Each run drops & recreates the public schema — never point at real data.
# Unset -> integration suites skip.
SUPABASE_TEST_DB_URL=
